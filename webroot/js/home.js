$(document).ready(function () {
    // Use Strict
    "use strict";

    // Iniciamos objeto home
    Home.init();
});

// Objeto home
let Home = {
    // Constructor
    init: function () {
        // LLamada a eventos
        this.events();
    },
    // Eventos
    events: function () {},
};
